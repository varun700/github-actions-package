import React from "react";
const index = ({ content }) => {
  return (
    <div>
      <button style={{ color: "green", backgroundColor: "yellow" }}>
        {content}
      </button>
    </div>
  );
};

export default index;
